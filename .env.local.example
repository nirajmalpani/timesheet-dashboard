# Required by next-auth. Generate with: `openssl rand -base64 32`
NEXTAUTH_SECRET=replace-me-with-a-long-random-string

# Optional. Defaults to http://localhost:3000 in dev.
# NEXTAUTH_URL=http://localhost:3000
